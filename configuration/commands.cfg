define command {
    command_name        cmd_plex
    command_line        $USERPLUGINSDIR$/plex/check_plex.py -H $HOSTADDRESS$ -p $_HOSTPLEX_PORT$ --use-ssl $_HOSTPLEX_USE_HTTPS$ --token $_HOSTPLEX_TOKEN$
}
